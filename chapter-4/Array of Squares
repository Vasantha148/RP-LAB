fn arr_square(numbers: Vec<i32>) -> Vec<i32> {
    let mut result = Vec::new();

    for n in numbers {
        result.push(n * n);
    }

    result
}

fn main() {
    let input = vec![1, 2, 3, 4, 5];
    let output = arr_square(input);

    println!("Squares: {:?}", output);
}
